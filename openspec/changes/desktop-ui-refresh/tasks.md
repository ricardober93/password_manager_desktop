## 1. Visual Foundation

- [x] 1.1 Define the desktop visual direction as reusable theme tokens for color, typography, spacing, radii, and feedback states
- [x] 1.2 Extract shared presentation primitives for app shell, section headers, action rows, empty states, and message surfaces
- [x] 1.3 Add any approved font or icon dependencies needed by the new visual system and wire them into the app theme

## 2. Entry Flows

- [x] 2.1 Redesign the first-run setup screen to match the new minimal security-focused visual language
- [x] 2.2 Redesign the unlock screen with the same visual system and clearer locked-state hierarchy
- [x] 2.3 Update widget coverage for setup and unlock flows so the refreshed layouts preserve current behavior

## 3. Unlocked Desktop Shell

- [x] 3.1 Replace the unlocked single-column layout with a desktop-first multi-panel shell
- [x] 3.2 Rebuild credential browsing around persistent search, list selection, and a visible empty state within the new shell
- [x] 3.3 Add widget or integration coverage for selection, search, lock, and empty-state behavior in the refreshed shell

## 4. Detail and Edit Experience

- [x] 4.1 Move credential detail presentation from modal-only dialogs into a persistent or contextual detail panel
- [x] 4.2 Redesign add and edit flows to use in-context editing surfaces that align with the new shell
- [x] 4.3 Rework generator controls, copy actions, and destructive actions to match the quieter interaction hierarchy

## 5. Polish and Validation

- [x] 5.1 Replace banner-style feedback with restrained status messaging that fits the new visual system
- [x] 5.2 Validate the refreshed UI across supported desktop window sizes and interaction states
- [x] 5.3 Run or update widget/integration coverage for create, unlock, browse, CRUD, search, copy, and lock flows after the refresh
